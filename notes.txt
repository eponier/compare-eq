- In recent versions of Coq, there is [Scheme Boolean Equality] that produces
  only the boolean comparison function (no proof!)
- can we use native ints to speed up certain things?
- could be do better with size types? Indeed, currently, to pass the guard
  checker, we need to be more verbose than what we could expect.
